# Test Environment Configuration
NODE_ENV=test
PORT=3002

# Test Database Configuration (use different database for tests)
DATABASE_URL=postgresql://postgres:password123@localhost:5432/invoice_portal_test

# Redis Configuration (use different Redis DB for tests)
REDIS_URL=redis://localhost:6379/1

# Session Configuration
SESSION_SECRET=test-session-secret

# Disable external APIs in tests
STRIPE_SECRET_KEY=sk_test_fake_key_for_testing
SENDGRID_API_KEY=SG.fake_key_for_testing
FROM_EMAIL=test@example.com

# Frontend URL
FRONTEND_URL=http://localhost:3000

# Disable rate limiting in tests
RATE_LIMIT_WINDOW_MS=1
RATE_LIMIT_MAX_REQUESTS=1000
